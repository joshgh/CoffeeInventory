
import { percent } from 'coffee-inventory/helpers/percent';
import { module, test } from 'qunit';

module('Unit | Helper | percent');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = percent([42]);
  assert.ok(result);
});

